services:

  postgres:
    image: postgres:16-alpine
    container_name: sumvibes_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: sumvibes_user
      POSTGRES_PASSWORD: sumvibes_password
      POSTGRES_DB: sumvibes_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sumvibes_user -d sumvibes_db"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - sumvibes_network

  adminer:
    image: adminer:4
    container_name: sumvibes_adminer
    restart: unless-stopped
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - sumvibes_network

  mongodb:
    image: mongo:7
    container_name: sumvibes_mongodb
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: rootpassword
      MONGO_INITDB_DATABASE: sumvibes_mongo_db
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - sumvibes_network

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sumvibes_app
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql://sumvibes_user:sumvibes_password@postgres:5432/sumvibes_db
      MONGODB_URI: mongodb://root:rootpassword@mongodb:27017/sumvibes_mongo_db?authSource=admin
    volumes:
      - .:/app
      - /app/node_modules
    depends_on:
      postgres:
        condition: service_healthy
      mongodb:
        condition: service_healthy
    networks:
      - sumvibes_network

volumes:
  postgres_data:
  mongodb_data:

networks:
  sumvibes_network:
    driver: bridge